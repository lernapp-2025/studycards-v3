// COMPLETELY EMPTY - This forces a new build
// All Stripe functionality moved to stripe-client.ts and stripe-server.ts

export const PREMIUM_PRICE = 799;
export const PREMIUM_CURRENCY = 'EUR';

// Dummy export to prevent "empty file" warnings
export const STRIPE_FILE_VERSION = 'v2';